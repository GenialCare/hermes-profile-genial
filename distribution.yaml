name: genial
version: 0.1.0
description: "Profile padrão da Genial Care para o Hermes Agent — OpenRouter, MCPs corporativos e skills bundled."
hermes_requires: ">=0.20.0"
author: "Genial Care"
license: "Proprietary"

env_requires:
  - name: OPENROUTER_API_KEY
    description: "Chave da API do OpenRouter (solicite acesso no canal #construindo-com-ia)"
    required: true

distribution_owned:
  - SOUL.md
  - config.yaml
  - skills/
  - cron/
