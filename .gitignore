# Credenciais & segredos — NUNCA commitar
.env
.env.EXAMPLE
auth.json

# Estado e runtime do Hermes (se alguém testar localmente dentro deste diretório)
state.db
state.db-shm
state.db-wal
gateway.pid
gateway_state.json
processes.json
auth.lock
active_profile
.update_check
.mcp-discovery.lock

# Dados de usuário / cache (se alguém testar localmente dentro deste diretório)
memories/
sessions/
logs/
plans/
workspace/
home/
cron/
skills/
image_cache/
audio_cache/
document_cache/
browser_screenshots/
cache/
checkpoints/
pairing/
hooks/
skins/

# Artefatos de teste
/tmp/
*.zip
*.tar.gz
